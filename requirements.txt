# ── Dashboard (required) ───────────────────────────────────────────────────────
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.17.0

# ── JNTO scraper (works on Streamlit Cloud — downloads live Excel) ─────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
openpyxl>=3.1.0

# ── REIT extractor (for local use — requires PDF files in pdfs/) ───────────────
pdfplumber>=0.10.0
numpy>=1.24.0
